# 文档分块功能概述

**生成日期**: 2025-12-08  
**项目**: RAG Framework - 文档分块模块  
**分支**: 002-doc-chunking  

---

## 📋 目录

1. [功能概述](#1-功能概述)
2. [架构设计](#2-架构设计)
3. [技术栈](#3-技术栈)

---

## 1. 功能概述

### 1.1 核心功能
文档分块功能是RAG (Retrieval-Augmented Generation) 系统的关键组件，负责将已加载/解析的文档按照不同策略切分为适合向量化的文本块。

### 1.2 主要特性
- ✅ **6种分块策略**: 按字数、按段落、按标题、按语义、父子分块、混合分块
- ✅ **父子分块**: 生成两层结构（父块提供上下文，子块用于检索），提升 RAG 效果
- ✅ **混合分块**: 针对不同内容类型（正文、代码、表格、图片）应用最合适的策略
- ✅ **多模态支持**: 独立提取表格、图片、代码块，支持多模态向量化
- ✅ **语义分块增强**: 支持 Embedding 模型（bge-m3、qwen3-embedding-8b、hunyuan-embedding），三级降级机制
- ✅ **图片分块优化**: 采用占位符+结构化元数据方案，包含 base64 数据用于多模态嵌入
- ✅ **参数可配置**: 每种策略支持灵活的参数调整
- ✅ **智能推荐**: 根据文档特征自动推荐最佳分块策略
- ✅ **实时预览**: 显示预计生成的文本块数量和效果对比
- ✅ **结果管理**: 查看、对比、删除、导出历史分块结果
- ✅ **版本控制**: 支持多版本管理、版本对比和回滚
- ✅ **性能优化**: 分页加载、异步处理、流式分块、结果缓存

### 1.3 设计原则
1. **模块化**: 各分块策略独立实现，易于扩展
2. **持久化**: 结果保存为JSON格式，支持索引和查询
3. **用户友好**: 直观的UI、实时反馈、智能推荐
4. **高性能**: 异步处理、队列管理、分页加载

---

## 2. 架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Vue3)                          │
├─────────────────────────────────────────────────────────────┤
│  ChunkingView.vue                                            │
│  ├── DocumentSelector      │  选择文档                       │
│  ├── StrategySelector      │  选择策略                       │
│  ├── ParameterConfig       │  配置参数                       │
│  ├── ChunkingProgress      │  进度显示                       │
│  ├── ChunkList             │  块列表                         │
│  ├── ChunkDetail           │  块详情                         │
│  └── HistoryList           │  历史记录                       │
└─────────────────────────────────────────────────────────────┘
                               ↕ (REST API)
┌─────────────────────────────────────────────────────────────┐
│                     API层 (FastAPI)                          │
├─────────────────────────────────────────────────────────────┤
│  chunking.py              │  分块相关接口                     │
│  chunking_history.py      │  历史管理接口                     │
└─────────────────────────────────────────────────────────────┘
                               ↕
┌─────────────────────────────────────────────────────────────┐
│                     服务层 (Service)                          │
├─────────────────────────────────────────────────────────────┤
│  chunking_service.py      │  核心分块业务逻辑                 │
│  ├── load_source_document │  加载文档                        │
│  ├── validate_parameters  │  验证参数                        │
│  ├── process_chunking     │  执行分块                        │
│  └── save_result          │  保存结果                        │
└─────────────────────────────────────────────────────────────┘
                               ↕
┌─────────────────────────────────────────────────────────────┐
│                   分块策略层 (Chunkers)                       │
├─────────────────────────────────────────────────────────────┤
│  base_chunker.py          │  基类                            │
│  character_chunker.py     │  按字数分块                       │
│  paragraph_chunker.py     │  按段落分块                       │
│  heading_chunker.py       │  按标题分块                       │
│  semantic_chunker.py      │  按语义分块                       │
└─────────────────────────────────────────────────────────────┘
                               ↕
┌─────────────────────────────────────────────────────────────┐
│                    数据层 (Models + Storage)                  │
├─────────────────────────────────────────────────────────────┤
│  数据库 (SQLite/PostgreSQL)                                  │
│  ├── chunking_tasks       │  分块任务                        │
│  ├── chunking_results     │  分块结果                        │
│  ├── chunking_strategies  │  策略配置                        │
│  └── chunks               │  具体文本块                      │
│                                                              │
│  文件系统                                                     │
│  └── results/chunking/    │  JSON结果文件                    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 模块职责

| 模块 | 职责 | 主要文件 |
|------|------|---------|
| **前端UI** | 用户交互、数据展示 | `ChunkingView.vue`及子组件 |
| **API层** | 接口暴露、请求处理 | `chunking.py`, `chunking_history.py` |
| **服务层** | 业务逻辑、流程控制 | `chunking_service.py` |
| **策略层** | 具体分块算法实现 | `*_chunker.py` |
| **数据层** | 数据持久化、查询 | Models + SQLAlchemy |

---

## 3. 技术栈

### 3.1 后端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.11+ | 后端语言 |
| FastAPI | 0.104.1 | Web框架 |
| SQLAlchemy | 2.0.23 | ORM |
| Pydantic | 2.0+ | 数据验证 |
| langchain-text-splitters | 0.3.4 | 语义分块支持 |

### 3.2 前端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.3.8 | UI框架 |
| Pinia | 2.1.7 | 状态管理 |
| TDesign Vue Next | 1.13.1 | 组件库 |
| Axios | 1.6.2 | HTTP客户端 |
| Vite | 5.0.0 | 构建工具 |

### 3.3 数据库

- **开发环境**: SQLite
- **生产环境**: PostgreSQL (推荐)
- **存储结构**: 关系型数据库 + 文件系统

---

## 4. 项目结构

### 4.1 后端目录结构

```
backend/src/
├── api/
│   ├── chunking.py              # 分块API端点
│   └── chunking_history.py      # 历史管理API
├── services/
│   └── chunking_service.py      # 核心分块服务
├── providers/
│   └── chunkers/
│       ├── base_chunker.py      # 基础分块器
│       ├── character_chunker.py # 按字数分块
│       ├── paragraph_chunker.py # 按段落分块
│       ├── heading_chunker.py   # 按标题分块
│       └── semantic_chunker.py  # 按语义分块
├── models/
│   ├── chunking_task.py         # 任务模型
│   ├── chunking_result.py       # 结果模型
│   ├── chunking_strategy.py     # 策略模型
│   └── chunk.py                 # 文本块模型
└── utils/
    ├── chunking_validators.py   # 参数验证
    └── chunking_helpers.py      # 辅助函数
```

### 4.2 前端目录结构

```
frontend/src/
├── views/
│   └── ChunkingView.vue         # 主视图
├── components/chunking/
│   ├── DocumentSelector.vue     # 文档选择器
│   ├── StrategySelector.vue     # 策略选择器
│   ├── ParameterConfig.vue      # 参数配置
│   ├── ChunkingProgress.vue     # 进度显示
│   ├── ChunkList.vue            # 块列表
│   ├── ChunkDetail.vue          # 块详情
│   └── HistoryList.vue          # 历史记录
├── stores/
│   └── chunkingStore.js         # 状态管理
└── services/
    └── chunkingService.js       # API调用封装
```

